const PageBasedFormHeader = ({ headerTitle }) => {
    return (
        <div className="form-header">
            <h2 className="mb-0">{headerTitle}</h2>
        </div>
    );
};

export default PageBasedFormHeader;
